extends ../../include/_layout
block var
  - title = 'computed'
  - path = '../'
block body
  h1 算出プロパティとウォッチャ
  a(href="https://jp.vuejs.org/v2/guide/computed.html", target="_blank") 算出プロパティとウォッチャ（Vue.js公式ドキュメント）

  h2 算出プロパティ
  #app
    p {{ message }}
    p {{ reverseMessage }}
    p {{ reverseMessage2() }}

  h2 算出プロパティ vs 監視プロパティ
  #app2
    p {{ fullName }}
    p {{ fullName2 }}
  //h2 データ属性にリアルタイムの時間を挿入する
  //#app2
  //  span(v-bind:title="message") テキストをマウスオーバーするとリアルタイムの時間が表示される
  //
  //h2 条件分岐で要素を表示、非表示にする
  //#app3
  //  p(v-if="seen") Now you see me
  //  button(v-on:click="toggleSeen") toggleSeen
  //
  //h2 ループでリストを表示
  //#app4
  //  ul
  //    li(v-for="todo in todos") {{todo.text}}
  //
  //h2 ボタンをクリック時、文字を反転する
  //#app5
  //  p {{message}}
  //  button(v-on:click="reverseMessage") Reverse Message
  //
  //h2 双方向バインディング
  //#app6
  //  p {{message}}
  //  input(v-model="message")
  //
  //h2 コンポーネント
  //#app7
  //  ul
  //    //v-bindはHTML属性をリアクティブに更新できる
  //    //例えばimg(src="{{imageName}}")
  //    //ではなく
  //    //img(v-bind:src="imageName")が正しい
  //    todo-item(v-for="item in groceryList" v-bind:todo="item")